// bus.proto
syntax = "proto3";

package bus;

service BusService {
  rpc DispatchRequest (BusPayload) returns (BusResponse);
}

message BusPayload {
  string action = 1;
  string payload_json = 2;
}

message BusResponse {
  int32 status_code = 1;
  string result_json = 2;
}